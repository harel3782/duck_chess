# Duck Chess — Python dependencies
# Install with:  pip install -r requirements.txt
#
# Versions below are pinned to what this project's .venv (Python 3.12) actually
# has installed and runs against. A single file is used on purpose: the web UI
# loads MaskablePPO models and imports the game engine, so it needs the full
# RL/engine stack — there is no runnable "web-only" subset.

# ── Web UI (FastAPI backend in web_ui/server.py) ───────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4

# ── RL / training stack (also required by the web UI to load models) ───────
stable-baselines3==2.8.0
sb3-contrib==2.8.0
gymnasium==1.2.3

# ── Numerics / tensors ─────────────────────────────────────────────────────
numpy==2.4.4
torch==2.11.0          # CPU build; for GPU install torch from the CUDA index

# ── Game engine ────────────────────────────────────────────────────────────
# Required (not optional): the headless engine imports pygame via turn_manager.py,
# so even the web server and training pull it in.
pygame==2.6.1

# ── Dev / test (optional) ──────────────────────────────────────────────────
# Needed only to run the test suite / web E2E tooling, not to play or train.
pytest==9.0.3
pytest-asyncio==1.4.0
pytest-watch==4.2.0
httpx==0.28.1
requests==2.34.2
playwright==1.60.0
# pytest-cov is NOT currently installed in .venv; install it if you want
# coverage:  pip install pytest-cov   (then: pytest --cov=DuckChess_Game.Logic)

# Note: Python 3.12 recommended.
#   python -m venv .venv
#   .venv\Scripts\activate            (Windows)   |   source .venv/bin/activate  (Linux/macOS)
#   pip install -r requirements.txt
