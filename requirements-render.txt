# Duck Chess — production dependencies for Render deployment
# Install with:  pip install -r requirements-render.txt
#
# Excludes dev/test tools (pytest, httpx, playwright, pytest-watch)
# that are not needed at runtime.

# ── Web UI (FastAPI backend) ───────────────────────────────────────────────
fastapi==0.136.3
uvicorn[standard]==0.49.0
pydantic==2.13.4

# ── RL stack (required to load MaskablePPO model checkpoints) ─────────────
stable-baselines3==2.8.0
sb3-contrib==2.8.0
gymnasium==1.2.3

# ── Numerics / tensors ─────────────────────────────────────────────────────
numpy==2.4.4
torch==2.11.0          # CPU build

# ── Model download (used by ensure_models_downloaded in server.py) ─────────
requests==2.34.2
